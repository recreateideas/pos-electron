import styled from 'styled-components';
import { Col } from 'react-bootstrap';

const Container = styled.div`
    display: flex;
`;

const BSCol = styled(Col)`
    padding: 0%;
`;

export { Container, BSCol };
