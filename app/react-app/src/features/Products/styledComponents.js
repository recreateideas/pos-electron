import styled, { css } from 'styled-components';

const Container = styled.div`
    ${props => {
        const {
            theme: {}
        } = props;
        return css``;
    }}
`;

export { Container };
